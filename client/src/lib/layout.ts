/**
 * Shared responsive layout: full width on small screens, capped and padded on larger.
 */
export const appShellClassName =
  "w-full min-h-dvh mx-auto px-4 sm:px-6 lg:px-8 max-w-full sm:max-w-2xl lg:max-w-4xl xl:max-w-6xl 2xl:max-w-7xl"

/** Bottom nav bar max width matches app shell breakpoints */
export const fluidNavBarClassName =
  "w-full max-w-full sm:max-w-2xl lg:max-w-4xl xl:max-w-6xl 2xl:max-w-7xl"
